# Fleet submodule

This repo is a submodule of Mattia's fleet repo (ServerCLI) — on the Mac mini it
lives at `~/git/ServerCLI/campfire`, on its box at `/home/ubuntu/campfire`.

**Read `../CLAUDE.md` before working here** — the fleet's agent instructions
(deploy flow, guardrails, shared utilities) live there.
